extends ../layout

block content
  link(rel="stylesheet", href="/css/signin.css")

  body
    .container-fluid.mainLogo
      .container
        .col-md-8.col-xs-0
          .col-md-3.col-xs-4
            img.img-responsive(src='/imgs/logo.png')
          .col-md-9.col-xs-8
            h3.text-left.title=title
        .col-md-4.col-xs-0.daidaidongdong
          img.img-responsive.gif(src='/imgs/xiaoxun.gif')
    br

    .container
      .row
        .form-inline
          .form-group
            select.form-control#find(type='text', onclick="change()")
              option 学生学号
              option 学生姓名
              option 项目类别
          .form-group.searchBox(style="display: none")
            input.form-control.searchInput#searchInput(type="text")
          .form-group.selectBox(style="display: none")
            select.form-control#searchKind(type='text')
              option.firstOption 自主学习类
              option 创新学分类
              option 养成体验类
              option 其他
          button.btn.btn-primary.searchBtn 查询
          button.btn.btn-danger.cleanBtn 重置
          &nbsp;&nbsp;&nbsp;
          button.btn.btn-warning.sortBtn 按时间排序
          button.btn.btn-danger.quit 注销
        br

      .row
        .panel.panel-primary
          .panel-heading(style="background-color:#1295d8")
            span.glyphicon.glyphicon-th-list(aria-hidden="true") 
            span  所有学生
          .panel-body
            -if (students.length==0)
              .alert.alert-warning 无用户
            -else
              .table-responsive
                table.table.table-condensed.table-hover
                  for item in students
                    tr.active
                      td.active 姓名：#{item.name}
                      td.active 性别：#{item.gender}
                      td.active 学号：#{item.id}
                      td.active 密码：#{item.password}
                      td.active 学院：#{item.faculty}
                      td.active 专业：#{item.major}
                      td.active 等级：#{item.level}

      .row
        .panel.panel-primary
          .panel-heading(style="background-color:#1295d8")
            span.glyphicon.glyphicon-th-list(aria-hidden="true") 
            span  所有项目
          .panel-body
            -if (projects.length==0)
              .alert.alert-warning 暂无相关项目
            -else
              .table-responsive
                table.table.table-condensed.table-hover.table-bordered
                  tr.active.allTr
                    td.active 姓名
                    td.active 类别
                    td.active 名称
                    td.active 日期
                    td.active 申请分值
                    td.active 认定分值
                    td.active 说明
                  for item in projects
                    tr(id="#{item.id}")
                      for stu in students
                        - if(stu.id==item.id)
                            td.stuName #{stu.name}
                      td.stuKind #{item.kind}
                      td #{item.name}
                      td #{item.date}
                      td #{item.applyPoint}
                      td #{item.finalPoint}
                      td #{item.remark}
      

    script(src="/libs/jquery/dist/jquery.min.js")
    script(src="/js/search.js")
    script(src="/js/signout.js")
